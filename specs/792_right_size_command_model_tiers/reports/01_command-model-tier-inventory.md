# Research Report: Task 792 — Right-sizing `model:` frontmatter across slash-commands

**Session**: sess_1782868693_adc8b1
**Task type**: meta
**Status**: Research complete

## 1. Complete inventory (`.claude/commands/*.md`, canonical source in `.claude/extensions/*/commands/`)

| Command | Current `model:` | Classification | Rationale (per `agent-frontmatter-standard.md`) |
|---|---|---|---|
| `/research` | `opus` | **KEEP-OPUS** | Context-accumulating orchestrator (explicit carve-out). Dispatches to sonnet-tier `general-research-agent`; own reasoning is orchestration-depth. Out of scope. |
| `/plan` | `opus` | **KEEP-OPUS** | Same orchestrator carve-out; dispatches to opus-tier `planner-agent`. Out of scope. |
| `/implement` | `opus` | **KEEP-OPUS** | Same orchestrator carve-out; dispatches to sonnet-tier `general-implementation-agent`. Out of scope. |
| `/orchestrate` | `opus` | **KEEP-OPUS** | Drives the full research→plan→implement state machine itself; explicitly kept per scope. |
| `/meta` | `opus` | **KEEP-OPUS** | Delegates to `meta-builder-agent` (opus tier — Deep Reasoning). System-architecture decisions. |
| `/revise` | `opus` | **KEEP-OPUS** | Delegates to `reviser-agent` (opus tier). Plan revision requires synthesizing prior research. |
| `/fix-it` | `opus` | **OMIT-FIELD** | `allowed-tools: Skill` only — pure delegation to `skill-fix-it` (direct-execution skill, no agent/model). |
| `/spawn` | `opus` | **OMIT-FIELD** | Delegates to `skill-spawn` → `spawn-agent` (`model: sonnet`). Omit so the agent's tier is not overridden. |
| `/project-overview` | `opus` | **OMIT-FIELD** | `allowed-tools: Skill` only — pure delegation to `skill-project-overview` (direct execution). |
| `/refresh` | `opus` | **OMIT-FIELD** | Pure delegation to `skill-refresh` (direct-execution skill); trivial top-level logic. |
| `/tag` | `opus` | **OMIT-FIELD** | Delegates to `skill-tag` (direct execution); user-only, mechanical git tag/push. |
| `/vet` | `opus` | **OMIT-FIELD** | Invokes `skill-cslib-vet` → `cslib-vet-agent` (`model: sonnet`). Don't override the agent tier. |
| `/merge` | `opus` | **DOWNGRADE-TO-SONNET** | No Skill/Agent dispatch — fully inline git/gh/glab + AskUserQuestion. User-only, mechanical. |
| `/errors` | `opus` | **DOWNGRADE-TO-SONNET** | `Agent` only in `allowed-tools`, never invoked — errors.json analysis + TaskCreate happen inline. |
| `/task` | `opus` | **DOWNGRADE-TO-SONNET** | Fully inline CRUD over state.json/TODO.md. No dispatch. |
| `/todo` | `opus` | **DOWNGRADE-TO-SONNET** | Fully inline archive/CHANGE_LOG logic, no dispatch. |
| `/review` | `opus` | **DOWNGRADE-TO-SONNET** | Fully inline grep/tier-scoring + AskUserQuestion; code-reviewer-agent not wired in. |
| `/pr` (cslib) | `opus` | **DOWNGRADE-TO-SONNET** | No dispatch in its own body — inline bash: branch, cache, CI pipeline, PR submission. |

Files already without `model:` (no change): `distill.md`, `learn.md`, `literature.md`, `zulip.md`, `README.md`. `zotero.md` is a broken symlink (ignore). `cite.md` not deployed into `.claude/commands/` (not live inventory).

**Count check**: exactly 18 files carry `model: opus` (6 KEEP-OPUS + 12 downgrade candidates), matching the estimate.

## 2. Generation-source mapping (nvim tree)

All command files in `.claude/commands/*.md` are **generated** (copied or symlinked) from extension sources — none are hand-authored in `.claude/commands/`:

- **Copy-based** (regenerated by loader `copy_simple_files`, byte-identical to source): `errors, fix-it, implement, merge, meta, orchestrate, plan, project-overview, refresh, research, review, revise, spawn, tag, task, todo, zulip` → source `.claude/extensions/core/commands/{name}.md`; `distill, learn` → `.claude/extensions/memory/commands/{name}.md`.
- **Symlinks**: `pr.md`, `vet.md` → `.claude/extensions/cslib/commands/`; `literature.md` → `.claude/extensions/literature/commands/`.

**Implementer must edit the extension sources**, not `.claude/commands/`:
- `.claude/extensions/core/commands/{errors,fix-it,merge,meta,orchestrate,plan,project-overview,refresh,research,review,revise,spawn,tag,task,todo}.md` (15 of 18)
- `.claude/extensions/cslib/commands/{pr,vet}.md` (2 of 18)

Copy-based files edited only at `.claude/commands/` are silently reverted on next resync.

**Root cause**: `.claude/extensions/core/commands/meta.md` (~line 184) has an embedded "Command Template Reference" hardcoding `model: opus` unconditionally, so every command scaffolded via `/meta` inherited opus. This template must be fixed too or the problem recurs.

**Stale docs needing reconciliation**:
- `.claude/docs/guides/creating-commands.md` (lines ~52-73): documents a contradictory model table (says dispatch commands should be sonnet, utility opus — opposite of this task).
- `.claude/docs/templates/command-template.md` (line 5): already `model: sonnet` — disagrees with meta.md's embedded template.

## 3. Dotfiles tree

`/home/benjamin/.dotfiles/.claude/` command files are **also generated** from the same canonical nvim-tree extension sources (its `extensions.json` `source_dir` points back into the nvim tree; dotfiles vendors no `commands/` payload). Dotfiles has 14 opus-tagged files (nvim set minus `pr`/`vet`/`literature`, since cslib/literature extensions aren't loaded there).

**Correction to task framing**: dotfiles is NOT independent hand-edit content — it is driven by the same Neovim-picker sync, just against a different target project on its own schedule. Edit the canonical `.claude/extensions/core/commands/*.md` ONCE, then run the picker resync **twice** (once per project). **Flag for user**: two separate picker resyncs required.

## 4. Load-bearing `model` values — NONE

Grepped every downgrade-candidate's full body for `opus` rationale/comments: zero occurrences beyond the frontmatter line in all 12 candidates. Safe to downgrade/omit all 12 with no exceptions.

## Key file paths

- Policy: `.claude/docs/reference/standards/agent-frontmatter-standard.md`
- Edit (nvim canonical): `.claude/extensions/core/commands/{errors,fix-it,merge,meta,orchestrate,plan,project-overview,refresh,research,review,revise,spawn,tag,task,todo}.md`, `.claude/extensions/cslib/commands/{pr,vet}.md`
- Stale docs: `.claude/docs/guides/creating-commands.md` (52-73), `.claude/docs/templates/command-template.md` (5), embedded template in `.claude/extensions/core/commands/meta.md` (~184)
- Dotfiles (do not hand-edit; resync): `/home/benjamin/.dotfiles/.claude/commands/*.md`
