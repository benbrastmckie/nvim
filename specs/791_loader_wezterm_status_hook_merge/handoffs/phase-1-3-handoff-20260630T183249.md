# Phase 1 & 3 Handoff — Task 791

**Immediate Next Action**: Begin Phase 2 (add `.claude/extensions/core/merge-sources/settings-hooks.json` fragment and declare `merge_targets.settings` in `.claude/extensions/core/manifest.json`).

**Current State**: Wave 1 (Phases 1 and 3) completed. Plan file `specs/791_loader_wezterm_status_hook_merge/plans/01_loader-settings-merge-plan.md` is current — both phase headings marked `[COMPLETED]`, all checklist items checked.

**Key Decisions Made**:
- Phase 1: Reconciled `provides.hooks` in both `.claude/extensions/core/manifest.json` and `.opencode/extensions/core/manifest.json` to the full 14-file on-disk list (removed phantom `wezterm-clear-status.sh`; added `claude-stop-notify.sh`, `validate-meta-write.sh`, `wezterm-preflight-status.sh`, `wezterm-utils.sh`).
- Phase 3: Patched `reinject_loaded_extensions()` in `lua/neotex/plugins/ai/claude/commands/picker/operations/sync.lua` (~line 220) to unconditionally insert `"core"` at the front of `loaded_names` when absent, with an explanatory comment. Removed the now-unreachable `#loaded_names == 0` early-return guard as dead code (see plan deviation annotation on Phase 3 task 1).

**Deviations from Plan**:
- Phase 3, Task 1: altered — removed the now-dead `if #loaded_names == 0 then return end` guard after making `loaded_names` always non-empty. Annotated inline in plan file.

**What NOT to Try**: N/A — no failed approaches this wave.

**References**:
- Plan: `specs/791_loader_wezterm_status_hook_merge/plans/01_loader-settings-merge-plan.md`
- Current phase: Wave 1 (Phases 1, 3) complete; next is Phase 2 (wave 2), then Phase 4 (wave 3).
